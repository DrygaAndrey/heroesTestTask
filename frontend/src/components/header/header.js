
import './header.scss'

function Header() {
    return (
        <header>
            <h1>Superhero database</h1>
            <button>Add Hero</button>
        </header>
    );
}

export default Header;
