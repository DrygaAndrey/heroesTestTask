const express = require('express');
const app = express();

// Добавьте обработчики маршрутов и другую логику сервера здесь

// Запуск сервера
app.listen(8000, () => {
  console.log('Сервер запущен на порту 8000');
});